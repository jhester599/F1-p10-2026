name: 2026 Qualifying Predictions

on:
  workflow_dispatch:
    inputs:
      round_override:
        description: "Optional round number override for manual test runs (e.g., 2 for China)"
        required: false
        type: string
      force_rerun:
        description: "Force rerun even if prediction output for that round already exists"
        required: false
        default: false
        type: boolean
  schedule:
    # 2026-specific cron probes (qualifying +90/+120/+150/+180/+210/+240 minutes, UTC).
    # Probes start at qualifying+90 (not +60) because Ergast/FastF1 qualifying
    # results are rarely published that early, and continue every 30 minutes
    # through +240 to give delayed publication several real chances to be caught.
    # The Python gate below enforces the same qualifying+90..+240 window.
    # Note: GitHub cron has no year field; these entries will still trigger in future years,
    # but the runner script gates execution against the 2026 published schedule window.
    # R01 Australian +90m
    - cron: "30 6 7 3 *"
    # R01 Australian +120m
    - cron: "0 7 7 3 *"
    # R01 Australian +150m
    - cron: "30 7 7 3 *"
    # R01 Australian +180m
    - cron: "0 8 7 3 *"
    # R01 Australian +210m
    - cron: "30 8 7 3 *"
    # R01 Australian +240m
    - cron: "0 9 7 3 *"
    # R02 Chinese +90m
    - cron: "30 8 14 3 *"
    # R02 Chinese +120m
    - cron: "0 9 14 3 *"
    # R02 Chinese +150m
    - cron: "30 9 14 3 *"
    # R02 Chinese +180m
    - cron: "0 10 14 3 *"
    # R02 Chinese +210m
    - cron: "30 10 14 3 *"
    # R02 Chinese +240m
    - cron: "0 11 14 3 *"
    # R03 Japanese +90m
    - cron: "30 7 28 3 *"
    # R03 Japanese +120m
    - cron: "0 8 28 3 *"
    # R03 Japanese +150m
    - cron: "30 8 28 3 *"
    # R03 Japanese +180m
    - cron: "0 9 28 3 *"
    # R03 Japanese +210m
    - cron: "30 9 28 3 *"
    # R03 Japanese +240m
    - cron: "0 10 28 3 *"
    # R04 Miami +90m
    - cron: "30 21 2 5 *"
    # R04 Miami +120m
    - cron: "0 22 2 5 *"
    # R04 Miami +150m
    - cron: "30 22 2 5 *"
    # R04 Miami +180m
    - cron: "0 23 2 5 *"
    # R04 Miami +210m
    - cron: "30 23 2 5 *"
    # R04 Miami +240m
    - cron: "0 0 3 5 *"
    # R05 Canadian +90m
    - cron: "30 21 23 5 *"
    # R05 Canadian +120m
    - cron: "0 22 23 5 *"
    # R05 Canadian +150m
    - cron: "30 22 23 5 *"
    # R05 Canadian +180m
    - cron: "0 23 23 5 *"
    # R05 Canadian +210m
    - cron: "30 23 23 5 *"
    # R05 Canadian +240m
    - cron: "0 0 24 5 *"
    # R06 Monaco +90m
    - cron: "30 15 6 6 *"
    # R06 Monaco +120m
    - cron: "0 16 6 6 *"
    # R06 Monaco +150m
    - cron: "30 16 6 6 *"
    # R06 Monaco +180m
    - cron: "0 17 6 6 *"
    # R06 Monaco +210m
    - cron: "30 17 6 6 *"
    # R06 Monaco +240m
    - cron: "0 18 6 6 *"
    # R07 Barcelona-Catalunya +90m
    - cron: "30 15 13 6 *"
    # R07 Barcelona-Catalunya +120m
    - cron: "0 16 13 6 *"
    # R07 Barcelona-Catalunya +150m
    - cron: "30 16 13 6 *"
    # R07 Barcelona-Catalunya +180m
    - cron: "0 17 13 6 *"
    # R07 Barcelona-Catalunya +210m
    - cron: "30 17 13 6 *"
    # R07 Barcelona-Catalunya +240m
    - cron: "0 18 13 6 *"
    # R08 Austrian +90m
    - cron: "30 15 27 6 *"
    # R08 Austrian +120m
    - cron: "0 16 27 6 *"
    # R08 Austrian +150m
    - cron: "30 16 27 6 *"
    # R08 Austrian +180m
    - cron: "0 17 27 6 *"
    # R08 Austrian +210m
    - cron: "30 17 27 6 *"
    # R08 Austrian +240m
    - cron: "0 18 27 6 *"
    # R09 British +90m
    - cron: "30 16 4 7 *"
    # R09 British +120m
    - cron: "0 17 4 7 *"
    # R09 British +150m
    - cron: "30 17 4 7 *"
    # R09 British +180m
    - cron: "0 18 4 7 *"
    # R09 British +210m
    - cron: "30 18 4 7 *"
    # R09 British +240m
    - cron: "0 19 4 7 *"
    # R10 Belgian +90m
    - cron: "30 15 18 7 *"
    # R10 Belgian +120m
    - cron: "0 16 18 7 *"
    # R10 Belgian +150m
    - cron: "30 16 18 7 *"
    # R10 Belgian +180m
    - cron: "0 17 18 7 *"
    # R10 Belgian +210m
    - cron: "30 17 18 7 *"
    # R10 Belgian +240m
    - cron: "0 18 18 7 *"
    # R11 Hungarian +90m
    - cron: "30 15 25 7 *"
    # R11 Hungarian +120m
    - cron: "0 16 25 7 *"
    # R11 Hungarian +150m
    - cron: "30 16 25 7 *"
    # R11 Hungarian +180m
    - cron: "0 17 25 7 *"
    # R11 Hungarian +210m
    - cron: "30 17 25 7 *"
    # R11 Hungarian +240m
    - cron: "0 18 25 7 *"
    # R12 Dutch +90m
    - cron: "30 15 22 8 *"
    # R12 Dutch +120m
    - cron: "0 16 22 8 *"
    # R12 Dutch +150m
    - cron: "30 16 22 8 *"
    # R12 Dutch +180m
    - cron: "0 17 22 8 *"
    # R12 Dutch +210m
    - cron: "30 17 22 8 *"
    # R12 Dutch +240m
    - cron: "0 18 22 8 *"
    # R13 Italian +90m
    - cron: "30 15 5 9 *"
    # R13 Italian +120m
    - cron: "0 16 5 9 *"
    # R13 Italian +150m
    - cron: "30 16 5 9 *"
    # R13 Italian +180m
    - cron: "0 17 5 9 *"
    # R13 Italian +210m
    - cron: "30 17 5 9 *"
    # R13 Italian +240m
    - cron: "0 18 5 9 *"
    # R14 Spanish +90m
    - cron: "30 15 12 9 *"
    # R14 Spanish +120m
    - cron: "0 16 12 9 *"
    # R14 Spanish +150m
    - cron: "30 16 12 9 *"
    # R14 Spanish +180m
    - cron: "0 17 12 9 *"
    # R14 Spanish +210m
    - cron: "30 17 12 9 *"
    # R14 Spanish +240m
    - cron: "0 18 12 9 *"
    # R15 Azerbaijan +90m
    - cron: "30 13 25 9 *"
    # R15 Azerbaijan +120m
    - cron: "0 14 25 9 *"
    # R15 Azerbaijan +150m
    - cron: "30 14 25 9 *"
    # R15 Azerbaijan +180m
    - cron: "0 15 25 9 *"
    # R15 Azerbaijan +210m
    - cron: "30 15 25 9 *"
    # R15 Azerbaijan +240m
    - cron: "0 16 25 9 *"
    # R16 Singapore +90m
    - cron: "30 14 10 10 *"
    # R16 Singapore +120m
    - cron: "0 15 10 10 *"
    # R16 Singapore +150m
    - cron: "30 15 10 10 *"
    # R16 Singapore +180m
    - cron: "0 16 10 10 *"
    # R16 Singapore +210m
    - cron: "30 16 10 10 *"
    # R16 Singapore +240m
    - cron: "0 17 10 10 *"
    # R17 United States +90m
    - cron: "30 22 24 10 *"
    # R17 United States +120m
    - cron: "0 23 24 10 *"
    # R17 United States +150m
    - cron: "30 23 24 10 *"
    # R17 United States +180m
    - cron: "0 0 25 10 *"
    # R17 United States +210m
    - cron: "30 0 25 10 *"
    # R17 United States +240m
    - cron: "0 1 25 10 *"
    # R18 Mexican +90m
    - cron: "30 22 31 10 *"
    # R18 Mexican +120m
    - cron: "0 23 31 10 *"
    # R18 Mexican +150m
    - cron: "30 23 31 10 *"
    # R18 Mexican +180m
    - cron: "0 0 1 11 *"
    # R18 Mexican +210m
    - cron: "30 0 1 11 *"
    # R18 Mexican +240m
    - cron: "0 1 1 11 *"
    # R19 Brazilian +90m
    - cron: "30 19 7 11 *"
    # R19 Brazilian +120m
    - cron: "0 20 7 11 *"
    # R19 Brazilian +150m
    - cron: "30 20 7 11 *"
    # R19 Brazilian +180m
    - cron: "0 21 7 11 *"
    # R19 Brazilian +210m
    - cron: "30 21 7 11 *"
    # R19 Brazilian +240m
    - cron: "0 22 7 11 *"
    # R20 Las Vegas +90m
    - cron: "30 5 21 11 *"
    # R20 Las Vegas +120m
    - cron: "0 6 21 11 *"
    # R20 Las Vegas +150m
    - cron: "30 6 21 11 *"
    # R20 Las Vegas +180m
    - cron: "0 7 21 11 *"
    # R20 Las Vegas +210m
    - cron: "30 7 21 11 *"
    # R20 Las Vegas +240m
    - cron: "0 8 21 11 *"
    # R21 Qatar +90m
    - cron: "30 19 28 11 *"
    # R21 Qatar +120m
    - cron: "0 20 28 11 *"
    # R21 Qatar +150m
    - cron: "30 20 28 11 *"
    # R21 Qatar +180m
    - cron: "0 21 28 11 *"
    # R21 Qatar +210m
    - cron: "30 21 28 11 *"
    # R21 Qatar +240m
    - cron: "0 22 28 11 *"
    # R22 Abu Dhabi +90m
    - cron: "30 15 5 12 *"
    # R22 Abu Dhabi +120m
    - cron: "0 16 5 12 *"
    # R22 Abu Dhabi +150m
    - cron: "30 16 5 12 *"
    # R22 Abu Dhabi +180m
    - cron: "0 17 5 12 *"
    # R22 Abu Dhabi +210m
    - cron: "30 17 5 12 *"
    # R22 Abu Dhabi +240m
    - cron: "0 18 5 12 *"

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

concurrency:
  group: qualifying-predictions-2026
  cancel-in-progress: false

jobs:
  run-automation:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Preflight run gate
        id: preflight
        run: |
          python3 - <<'PY'
          import json
          from datetime import datetime, timezone, timedelta
          from pathlib import Path
          from urllib.request import urlopen
          import os

          event_name = os.getenv("GITHUB_EVENT_NAME", "")
          out_path = os.getenv("GITHUB_OUTPUT")

          def set_output(name: str, value: str) -> None:
              if not out_path:
                  return
              with open(out_path, "a", encoding="utf-8") as f:
                  f.write(f"{name}={value}\n")

          # Manual dispatch should always proceed; override logic lives in runner script.
          if event_name != "schedule":
              set_output("run_full", "true")
              set_output("skip_reason", "")
              raise SystemExit(0)

          year = 2026
          schedule_url = f"https://raw.githubusercontent.com/sportstimes/f1/main/_db/f1/{year}.json"
          now = datetime.now(timezone.utc)
          offset_minutes = 90
          window_minutes = 150

          with urlopen(schedule_url, timeout=30) as resp:
              payload = json.loads(resp.read().decode("utf-8"))

          in_window_round = None
          for race in payload.get("races", []):
              if race.get("canceled"):
                  continue
              q = race.get("sessions", {}).get("qualifying")
              if not q:
                  continue
              qdt = datetime.fromisoformat(q.replace("Z", "+00:00"))
              run_start = qdt + timedelta(minutes=offset_minutes)
              run_end = run_start + timedelta(minutes=window_minutes)
              if run_start <= now <= run_end:
                  in_window_round = int(race["round"])
                  break

          if in_window_round is None:
              set_output("run_full", "false")
              set_output("skip_reason", "Not in qualifying+90..+240 window.")
              raise SystemExit(0)

          out_csv = Path(f"results/prediction_{year}_R{in_window_round:02d}.csv")
          if out_csv.exists():
              set_output("run_full", "false")
              set_output(
                  "skip_reason",
                  f"Prediction already exists for {year} R{in_window_round:02d}; likely completed on earlier cron slot.",
              )
              raise SystemExit(0)

          set_output("run_full", "true")
          set_output("skip_reason", "")
          PY

      - name: Setup Python
        if: steps.preflight.outputs.run_full == 'true'
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Restore processed-data cache
        if: steps.preflight.outputs.run_full == 'true'
        id: cache_processed_restore
        uses: actions/cache/restore@v4
        with:
          path: data/processed
          key: processed-2026-${{ hashFiles('config.py', 'src/feature_engineering.py', 'scripts/02_build_dataset.py', 'requirements.txt') }}

      - name: Restore raw-cache extraction
        if: steps.preflight.outputs.run_full == 'true'
        id: cache_raw_restore
        uses: actions/cache/restore@v4
        with:
          path: data/raw
          key: rawcache-2026-${{ hashFiles('f1_data_cache_2026-03-09.zip') }}
          restore-keys: |
            rawcache-2026-

      - name: Restore model-artifact cache
        if: steps.preflight.outputs.run_full == 'true'
        id: cache_models_restore
        uses: actions/cache/restore@v4
        with:
          path: models
          key: models-2026-${{ hashFiles('config.py', 'src/models.py', 'scripts/03_train_models.py', 'requirements.txt') }}

      - name: Install dependencies
        if: steps.preflight.outputs.run_full == 'true'
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run post-qualifying automation
        if: steps.preflight.outputs.run_full == 'true'
        id: run_prediction
        run: |
          if [ "${{ github.event_name }}" = "schedule" ]; then
            python scripts/65_run_qualifying_automation.py \
              --year 2026 \
              --require-schedule-window \
              --offset-minutes 90 \
              --window-minutes 150
          else
            CMD="python scripts/65_run_qualifying_automation.py --year 2026"
            if [ -n "${{ inputs.round_override }}" ]; then
              CMD="$CMD --round ${{ inputs.round_override }}"
            fi
            if [ "${{ inputs.force_rerun }}" = "true" ]; then
              CMD="$CMD --force-rerun"
            fi
            echo "Running: $CMD"
            eval "$CMD"
          fi

      - name: Preflight skip notice
        if: steps.preflight.outputs.run_full != 'true'
        run: |
          echo "Skipping full automation run."
          echo "Reason: ${{ steps.preflight.outputs.skip_reason }}"

      - name: Upload prediction artifacts
        if: steps.run_prediction.outputs.new_prediction == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: prediction-2026-r${{ steps.run_prediction.outputs.round }}
          path: |
            ${{ steps.run_prediction.outputs.prediction_csv }}
            ${{ steps.run_prediction.outputs.report_path }}
            ${{ steps.run_prediction.outputs.season_log }}

      - name: Save processed-data cache
        if: steps.run_prediction.outputs.new_prediction == 'true' && steps.cache_processed_restore.outputs.cache-hit != 'true'
        uses: actions/cache/save@v4
        with:
          path: data/processed
          key: ${{ steps.cache_processed_restore.outputs.cache-primary-key }}

      - name: Save raw-cache extraction
        if: steps.run_prediction.outputs.new_prediction == 'true' && steps.cache_raw_restore.outputs.cache-hit != 'true'
        uses: actions/cache/save@v4
        with:
          path: data/raw
          key: ${{ steps.cache_raw_restore.outputs.cache-primary-key }}

      - name: Save model-artifact cache
        if: steps.run_prediction.outputs.new_prediction == 'true' && steps.cache_models_restore.outputs.cache-hit != 'true'
        uses: actions/cache/save@v4
        with:
          path: models
          key: ${{ steps.cache_models_restore.outputs.cache-primary-key }}

      - name: Commit rebuilt processed artifacts
        if: steps.preflight.outputs.run_full == 'true'
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore: persist rebuilt processed data [skip ci]"
          file_pattern: |
            data/processed/*.parquet

      - name: Stage prediction documentation
        if: steps.run_prediction.outputs.new_prediction == 'true'
        run: |
          git add -- \
            "${{ steps.run_prediction.outputs.prediction_csv }}" \
            "${{ steps.run_prediction.outputs.report_path }}" \
            "${{ steps.run_prediction.outputs.season_log }}"

      - name: Commit documentation updates
        if: steps.run_prediction.outputs.new_prediction == 'true'
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore: add 2026 R${{ steps.run_prediction.outputs.round }} qualifying prediction"
          file_pattern: |
            results/prediction_2026_R*.csv
            results/prediction_reports/*.md
            results/automated_predictions_2026.md

      - name: Check email secret configuration
        id: email_cfg
        if: steps.run_prediction.outputs.new_prediction == 'true'
        env:
          SMTP_SERVER: ${{ secrets.SMTP_SERVER }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_USERNAME: ${{ secrets.SMTP_USERNAME }}
          SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
          PREDICTION_EMAIL_TO: ${{ secrets.PREDICTION_EMAIL_TO }}
          PREDICTION_EMAIL_FROM: ${{ secrets.PREDICTION_EMAIL_FROM }}
        run: |
          if [ -n "$SMTP_SERVER" ] && [ -n "$SMTP_PORT" ] && [ -n "$SMTP_USERNAME" ] && [ -n "$SMTP_PASSWORD" ] && [ -n "$PREDICTION_EMAIL_TO" ] && [ -n "$PREDICTION_EMAIL_FROM" ]; then
            echo "email_ready=true" >> "$GITHUB_OUTPUT"
          else
            echo "email_ready=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Send prediction email
        if: steps.run_prediction.outputs.new_prediction == 'true' && steps.email_cfg.outputs.email_ready == 'true'
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: ${{ secrets.SMTP_SERVER }}
          server_port: ${{ secrets.SMTP_PORT }}
          username: ${{ secrets.SMTP_USERNAME }}
          password: ${{ secrets.SMTP_PASSWORD }}
          subject: ${{ steps.run_prediction.outputs.email_subject }}
          to: ${{ secrets.PREDICTION_EMAIL_TO }}
          from: ${{ secrets.PREDICTION_EMAIL_FROM }}
          body: ${{ steps.run_prediction.outputs.email_body }}

      - name: Missing email secrets notice
        if: steps.run_prediction.outputs.new_prediction == 'true' && steps.email_cfg.outputs.email_ready == 'false'
        run: |
          echo "Prediction generated, but email secrets are missing."
          echo "See docs/GITHUB_ACTIONS_AUTOMATION.md for required secrets."
